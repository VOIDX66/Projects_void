<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestionar Participación</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Gestionar Participación en Partidos</h1>
        <p class="text-center">Jugador: <strong><%= usuario.nombre %></strong></p>
        <h2 class="mt-4">Confirmar Baja y Reemplazo</h2>
        
        <form action="/dar_de_baja_participacion/confirmar" method="post">
            <input type="hidden" name="id_partido" value="<%= idPartido %>">
            <input type="hidden" name="id_jugador" value="<%= idJugador %>">
            
            <div class="form-group">
                <label for="id_reemplazo">Selecciona un jugador para reemplazo:</label>
                <select name="id_reemplazo" id="id_reemplazo" class="form-control">
                    <option value="">-- Ninguno (dar de baja sin reemplazo) --</option>
                    <% jugadoresDisponibles.forEach(jugador => { %>
                        <option value="<%= jugador.id_jugador %>"><%= jugador.nombre %></option>
                    <% }); %>
                </select>
            </div>

            <div class="mt-3">
                <button type="submit" class="btn btn-danger">Confirmar Baja</button>
            </div>
        </form>

        <button onclick="window.location.href='/';" class="btn btn-secondary mt-3">Volver Atrás</button>
    </div>

    <script>
        // Esta función se ejecuta cuando el formulario se envía.
        // Puede usarse para validaciones o agregar funcionalidades adicionales si es necesario.
    </script>
</body>
</html>